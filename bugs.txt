When logging in, and user name does not exist, gives php error: userName does not exist

EditResultSubmit is empty

New topic appears last in topic list, but should appear at the top

Rendered news events retrieve for example the title field of the current task, and not what the title was when the event was created

Join team already member of, creates duplicate names in list

In LeaveTeamSubmit hadnt set $this->taskId. The program hung when clicking the button.

Potential problem: $this->taskId is also set in setTaskParams. Collide?

Bug discovered: Subtasks show up on the main task page, but when clicing "Edit subtasks" there are now subtasks

Bug: Task status sometimes says undefined, and no radio buttons are selected - solved: Used capital letter "W" in Work in progress in CreateTaskSubmit, but should have been small letter "w"